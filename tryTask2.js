const hasPalindromeChild = require('./task2/hasPalindromeChild');

try {
    const result = hasPalindromeChild(121221); //Input here
    console.log(result);
} catch (err) {
    console.log('ERROR: ',err.message);
}
